"use strict"

class IMG {
    #image;

    constructor(res_id) {
        this.#image = res_id;
    }

    draw(ctx, x, y, rotate) {
        rotate = rotate || false;

    }

    drawVert(ctx, x, y) {

    }
}